import '../scss/contact.scss';

const Contact = () => {
    return (  
        <section className="contact">
            <div className="subTitle">
                <span>
                    CONTACT
                </span>
            </div>
        </section>
    );
};

export default Contact;